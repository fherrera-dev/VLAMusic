import { ErrorimgpipePipe } from './errorimgpipe.pipe';

describe('ErrorimgpipePipe', () => {
  it('create an instance', () => {
    const pipe = new ErrorimgpipePipe();
    expect(pipe).toBeTruthy();
  });
});
